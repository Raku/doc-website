=begin pod
=TITLE git-reflog is a plugin for Collection

The plugin is for the setup and render milestones.

Obtains the reflog from the local-raku repository and puts the hash value
into a template that can be accessed.

The setup callable generates the template with the hash value.

=head1 Templates
add git-reflog template

=end pod
